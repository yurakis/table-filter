import { DictionaryItemNamePipe } from './dictionary-item-name.pipe';

describe('DictionaryItemNamePipe', () => {
  it('create an instance', () => {
    const pipe = new DictionaryItemNamePipe();
    expect(pipe).toBeTruthy();
  });
});
